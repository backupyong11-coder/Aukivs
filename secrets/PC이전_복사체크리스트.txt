# PC 이전 — 반드시 수동 복사할 것 (Git 제외)
# 이 폴더(secrets/)는 .gitignore 대상입니다. 실제 비밀값은 여기 파일에 적지 마세요.

[ ] backend/.env
    - DATA_BACKEND=supabase
    - SUPABASE_URL
    - SUPABASE_SERVICE_ROLE_KEY  ← 절대 Git/Vercel/프론트 금지
    - (선택) GOOGLE_* 시트 연동
    - (선택) OPENAI_API_KEY

[ ] frontend/.env.local
    - DEMO_PIN
    - OPSPROXY_TARGET=http://127.0.0.1:8001  (로컬)
    - NEXT_PUBLIC_API_BASE_URL 는 비워 두기

[ ] Google 서비스 계정 JSON (시트 모드 사용 시)
    - backend/.env 의 GOOGLE_SERVICE_ACCOUNT_FILE 경로

[ ] (선택) C:\Users\<사용자>\.fly\config.yml
    - flyctl deploy 시 로그인 대체

[ ] Vercel / Fly / Supabase 대시보드 로그인
    - 같은 GitHub 계정으로 Vercel 연동 확인

[ ] Supabase SQL Editor 접속 (마이그레이션 확인용)
    - migrations/001 ~ 006 적용 여부

운영 URL 메모:
  Fly:  https://backend-patient-wave-707.fly.dev
  Git:  https://github.com/backupyong11-coder/Aukivs
