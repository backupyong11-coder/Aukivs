# 웹툰 운영 관제실 — 인수인계서

다음에 배포·로컬 실행·메뉴 오류로 헤매지 않도록 **이 프로젝트만** 정리한 문서입니다.  
상세 배포 클릭 가이드는 [README_DEPLOY_FINAL.md](./README_DEPLOY_FINAL.md), API 목록은 [FULL_HANDOFF_AND_NEXT_STEPS.md](./FULL_HANDOFF_AND_NEXT_STEPS.md)를 참고하세요.

---

## 1. 한 장 요약

```
[브라우저]
    ↓
Vercel (화면)  localhost:3000  또는  https://xxxx.vercel.app
    ↓  /api/ops/...  (Next.js rewrite)
Fly.io (API 서버)  https://backend-patient-wave-707.fly.dev
    ↓
Supabase (DB)  ← DATA_BACKEND=supabase 일 때 메모·업무·체크리스트 등
Google Sheets  ← 일부 API만 (platform-master, 레거시 uploads 등)
```

| 이름 | 하는 일 | 이 프로젝트에서 |
|------|---------|-----------------|
| **Supabase** | 데이터 저장(DB) | 메모·업무·업로드정리 등 **저장소** |
| **Fly.io** | FastAPI **API 서버** (인터넷) | 온라인 백엔드 |
| **Vercel** | Next.js **화면** | 온라인 프론트 |
| **로컬 8001** | FastAPI | 개발 시 API |
| **로컬 3000** | Next.js | 개발 시 화면 |

**헷갈리지 말 것:** Supabase만 쓴다고 Fly/Railway가 필요 없는 게 **아님**. 브라우저는 Supabase에 직접 안 붙고, **항상 API 서버(Fly 또는 로컬 8001)** 를 거침.

---

## 2. 실제 운영 주소 (2026-05 기준)

| 구분 | URL | 확인 방법 |
|------|-----|-----------|
| **온라인 API (Fly)** | `https://backend-patient-wave-707.fly.dev` | `/health` → `{"status":"ok",...}` |
| **온라인 화면 (Vercel)** | Vercel 대시보드 → Domains 에 표시된 주소 | PIN 로그인 후 관제실 |
| **로컬 화면** | http://localhost:3000 | |
| **로컬 API** | http://127.0.0.1:8001 | `/health` |

GitHub 저장소: `https://github.com/backupyong11-coder/Aukivs`

---

## 3. 로컬에서 켜기 (매번 이렇게)

터미널 **2개** 필요. 하나만 켜면 메뉴·삭제가 안 될 수 있음.

### 터미널 1 — 백엔드 (API)

```powershell
cd c:\Cording\Sheet_Work\WorkSheet\backend
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8001
```

`Application startup complete` 가 보일 때까지 대기.

### 터미널 2 — 프론트 (화면)

```powershell
cd c:\Cording\Sheet_Work\WorkSheet\frontend
npm run dev
```

`Local: http://localhost:3000` + `Ready` 확인.

### 브라우저

1. http://localhost:3000  
2. `frontend/.env.local` 의 `DEMO_PIN` 입력 (설정해 둔 경우)  
3. 메뉴 사용

### 로컬 env 권장 (`frontend/.env.local`)

```env
OPSPROXY_TARGET=http://127.0.0.1:8001
DEMO_PIN=본인이_정한_PIN
```

- **`NEXT_PUBLIC_API_BASE_URL` 은 비우기(삭제)** — 넣으면 브라우저가 8001로 직접 붙어서 배포와 동작이 달라짐.

### 백엔드 env (`backend/.env`)

`backend/.env.example` 복사 후 채움. Supabase 사용 시 최소:

```env
DATA_BACKEND=supabase
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...   # 절대 Git·프론트·Vercel에 넣지 말 것
```

---

## 4. 온라인 배포 (이 프로젝트 실제 구성)

**Railway/Render 안 씀.** 아래 두 곳만 보면 됨.

### A. 백엔드 — Fly.io (코드 올리기)

코드를 수정했으면 Fly에 **다시 배포**해야 온라인에 반영됨. Restart만으로는 코드 안 바뀜.

**방법 1 — PC에서 (flyctl 설치 후)**

```powershell
# 최초 1회 설치
powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
```

**PowerShell 창을 닫았다가 새로 연 뒤:**

```powershell
$env:Path += ";$env:USERPROFILE\.fly\bin"
flyctl auth login
cd c:\Cording\Sheet_Work\WorkSheet\backend
flyctl deploy
```

> `fly` 가 안 되면 **`flyctl`** 사용.  
> PATH 오타 주의: `$env:USERPROFILE` (**달러 `$` 필수**)

**방법 2 — Fly 웹**

https://fly.io/apps/backend-patient-wave-707 → Activity / Deploy

배포 후 확인: `https://backend-patient-wave-707.fly.dev/health`

### B. 프론트 — Vercel

1. https://vercel.com → 프로젝트 (루트 디렉터리 **`frontend`**)
2. **Settings → Environment Variables**
   - `OPSPROXY_TARGET` = `https://backend-patient-wave-707.fly.dev` (끝 `/` 없음)
   - `DEMO_PIN` = 공유할 PIN
   - `NEXT_PUBLIC_API_BASE_URL` = **설정하지 않음**
3. **Deployments → Redeploy**

### 다른 사람에게 줄 것

- **접속 URL:** Vercel 주소만  
- **PIN:** `DEMO_PIN` 과 동일  
- Fly URL·Supabase 키는 **공유하지 않음**

---

## 5. 코드 수정 후 체크리스트

| 수정한 곳 | 로컬 | 온라인 |
|-----------|------|--------|
| `backend/` Python | 백엔드 터미널 **재시작** (또는 `--reload`) | **`flyctl deploy`** |
| `frontend/` | `npm run dev` **재시작** | Vercel **Redeploy** |
| env만 변경 | 해당 터미널 재시작 + Vercel env 저장 후 Redeploy | |

**증상:** 로컬은 되는데 온라인만 안 됨 → Fly/Vercel 중 하나가 **옛 버전**인 경우가 대부분.

---

## 6. 자주 나오는 오류

### 「메모 저장/삭제 실패… /memos/append 연결 확인」

| 원인 | 해결 |
|------|------|
| 백엔드(8001) 안 켬 | 터미널 1에서 uvicorn 실행 |
| 백엔드 **옛 코드** (삭제 API 없음) | 백엔드 재시작 또는 Fly `deploy` |
| 온라인 Fly **29일 전 배포** 그대로 | `flyctl deploy` |
| Vercel `OPSPROXY_TARGET` 잘못됨 | Fly URL과 일치하는지 확인 |

메모 **삭제**는 `POST /memos/delete` (DELETE `/memos` 아님, 2026-05 반영).

### 챗봇·캘린더·플랫폼 메뉴가 비어 보임

- API: `GET /hub/chatbot-context`, `/hub/calendar-window`, `/hub/platform-matrix-bootstrap`
- 백엔드 미실행·Fly 미배포·`DEMO_PIN` 미로그인(401) 확인
- 브라우저 Network 탭에서 `/api/ops/hub/...` 상태 코드 확인

### `fly` / `flyctl` 명령을 찾을 수 없음

1. 설치 스크립트 실행  
2. **PowerShell 새로 열기**  
3. `$env:Path += ";$env:USERPROFILE\.fly\bin"`  
4. `flyctl` 로 실행

### 포트 3000 / 3001 충돌

- 예전 `npm run dev` 가 3000 점유 → `taskkill /PID (번호) /F` 후 다시 `npm run dev`
- **3000** 으로 뜨는지 확인

### `일괄 작업을 끝내시겠습니까 (Y/N)?`

- Ctrl+C 눌렀을 때 나옴 → 서버 유지: **N**

---

## 7. 주요 메뉴 ↔ API

| 화면 경로 | 데이터 로딩 (요약) |
|-----------|-------------------|
| `/` 관제실 | `/briefing/today`, hub 등 |
| `/memo` | `GET /memos`, 삭제 `POST /memos/delete` |
| `/chatbot` | `GET /hub/chatbot-context` |
| `/calendar` | `GET /hub/calendar-window?from_ymd=&to_ymd=` |
| `/platform-matrix` | `GET /hub/platform-matrix-bootstrap` |

프론트는 대부분 `getApiBaseUrl()` → `/api/ops` → `OPSPROXY_TARGET`(로컬이면 8001).

---

## 8. 보안 (반드시)

| 넣어도 되는 곳 | 넣으면 안 되는 곳 |
|----------------|------------------|
| `backend/.env` | `frontend/.env`에 Supabase service role |
| Fly Secrets / Railway Variables | Git 커밋 |
| Vercel: `OPSPROXY_TARGET`, `DEMO_PIN` only | Vercel: `SUPABASE_SERVICE_ROLE_KEY` |

---

## 9. 검증 명령 (빠른 점검)

```powershell
# 백엔드 살아 있나
curl.exe https://backend-patient-wave-707.fly.dev/health

# 로컬 백엔드
curl.exe http://127.0.0.1:8001/health

# 저장소 테스트 (백엔드 폴더)
cd backend
python -m pytest -q
```

```powershell
cd frontend
npm test
npm run build
```

---

## 10. 문서 맵

| 문서 | 언제 보나 |
|------|-----------|
| **이 파일 (`인수인계서.md`)** | 일상 운영·배포·트러블슈팅 |
| [README_DEPLOY_FINAL.md](./README_DEPLOY_FINAL.md) | 처음 클라우드 세팅할 때 |
| [README_DEMO.md](./README_DEMO.md) | PIN·프록시 개념 |
| [QUICK_START_VALIDATION.md](./QUICK_START_VALIDATION.md) | 자동 검증 스크립트 |

---

## 11. 다음 담당자에게 한 줄

**「Supabase = DB, Fly = 온라인 API, Vercel = 화면. 로컬은 8001+3000 두 터미널. 코드 바꿨으면 Fly deploy + Vercel Redeploy.»**

문서 수정일: 2026-05-18
